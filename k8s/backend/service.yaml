apiVersion: v1
kind: Service
metadata:
  name: backend-clientes-mklens
  labels:
    app: backend-clientes-mklens
spec:
  type: ClusterIP
  selector:
    app: backend-clientes-mklens
  ports:
    - name: http
      port: 3080
      targetPort: http
